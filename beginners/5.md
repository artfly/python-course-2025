# Задание 5

Подумали вы, подумали и решили - а ведь ваш парень программист не так уж плох!
Валентинки дарит, в любви признается... еще и с английским может помочь

Поэтому вы решили, что тоже сделаете ему приятно: решите за него его домашку по программированию!
Ну или, по крайней мере, попытаетесь...

Домашка, к счастью, была небольшая. Вот она:

### 1. indexed

Напишите функцию `indexed`, которая принимает на вход список и возвращает список кортежей вида (индекс, элемент_списка).
То есть работает также, как `enumerate` в Python.

#### Объявление функции

```python
def indexed(elements):
    pass
    # TODO: здесь нужно написать ваш код
```

---

#### Пример использования

```python
>>> products = ['авокадо', 'кола', 'пельмени']
>>> indexed(products)
[(0, 'авокадо'), (1, 'кола'), (2, 'пельмени')]
```

---

#### Требования

- Алгоритмическая сложность — `O(n)`. 
- Использовать `enumerate` из Python нельзя.


### 2. search_word

Напишите функцию `search_word`, которая получает на вход название файла и слово, которое нужно искать. 
Функция должна возвращать список позиций начал слова.

#### Объявление функции

```python
def search_word(fname, word):
    pass
    # TODO: здесь нужно написать ваш код
```

---

#### Пример использования

```
>>> # файл file.txt содержит в себе строку "ананас"
>>> search_word("file.txt", "ан")
[0, 2]
```

---

#### Требования

- Алгоритмическая сложность — `O(n)`.
