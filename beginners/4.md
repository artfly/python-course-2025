# Задание 4

### 1. Я устал

После игр в волейбол книжный клуб собрался обсудить не только результат, но и самочувствие 🏐  
Каждый участник оценил **свою усталость по шкале от 1 до 5**, где  
`1` — "мог бы играть еще два часа",  
а `5` — "вынесите меня с площадки".

Теперь вы хотите узнать распределение этих оценок — сколько человек выбрали каждый уровень.

---

#### Задача

Напишите функцию `counting_tiredness(levels)`, которая:

1. Принимает список целых чисел `levels` — оценки усталости игроков.  
2. Подсчитывает, сколько раз встретился каждый уровень.  
3. Выводит уровни усталости **в порядке от 1 до 5**, повторяя каждое число столько раз, сколько человек его выбрало.  

---

#### Требования

- Диапазон возможных значений — от `1` до `5`.  
- Алгоритмическая сложность — `O(n)`. 
- Использовать стандартную сортировку Python нельзя.

---

#### Объявление функции

```python
def counting_tiredness(levels):
    pass
    # TODO: здесь нужно написать ваш код
```

---

#### Пример использования

```python
>>> tiredness = [5, 3, 4, 5, 2, 1, 3, 5, 4, 5]
>>> counting_tiredness(tiredness)
1 2 3 3 4 4 5 5 5 5
```

#### Подсказки

- Создайте список-счётчик из 5 элементов (для уровней 1–5).
- Для каждого значения увеличивайте соответствующий счётчик.
- Затем выведите уровни в порядке возрастания, столько раз, сколько они встречались.

### 2. Кофе мой друг

После книжного клуба участники пошли в **гастробистро** — пробовать новые блюда и обсуждать, какое круче всего.  
Каждый участник заказывает блюдо и вы записываете всё в список.  
Но потом захотелось узнать: **а сколько разных блюд вообще попробовали за вечер?**

---

#### Задача

Напишите функцию `unique_dishes(dishes)`, которая:

1. Принимает список строк `dishes` — названия блюд, которые участники принесли.  
2. Возвращает количество **уникальных** блюд.

---

#### Требования

- Сложность алгоритма — `O(n)` 

---

#### Объявление функции

```python
def unique_dishes(dishes):
    pass
    # TODO: здесь нужно написать ваш код
```

---

#### Пример использования

```python
>>> dishes = ["паста", "борщ", "паста", "греческий салат", "тирамису", "борщ"]
>>> unique_dishes(dishes)
3
```

---

#### Подсказки

- Возможно, вам нужна какая-то определенная коллекция?

### 3. I like to move it


На этот раз участники книжного клуба решили, что хватит обсуждать только тексты — пора устроить **киновечер**!  
Каждый участник по очереди называет фильм, который хочет предложить к просмотру, и его рейтинг с IMDB.

После каждого нового предложения хочется знать, какой фильм сейчас лидирует — у кого рейтинг выше всех.  
Вы будете вести этот список прямо в коде.

---
#### Задача

Напишите функцию `movie_night()`, которая:

1. В бесконечном цикле (`while True:`)  
   запрашивает у пользователя строку вида:  
   `"Название_фильма рейтинг"`

   Например: `Интерстеллар 9`

2. Выводит, какой сейчас максимальный рейтинг
---

#### Требования

- Алгоритмическая сложность добавления фильма — `O(1)`  
- Алгоритмическая сложность показа лучшего фильма — `O(1)`  
- Названия фильмов — строки, рейтинги — дробные числа 

---

#### Объявление функции

```python
def movie_night():
    pass
    # TODO: здесь нужно написать ваш код
```
---

#### Пример использования

```python
>>> movie_night()
Введите фильм и рейтинг:
Дюна 8.0
Сейчас лидирует: Дюна (8.0)

Введите фильм и рейтинг:
Интерстеллар 8.7
Сейчас лидирует: Интерстеллар (8.7)

Введите фильм и рейтинг:
Убить Билла 8.2
Сейчас лидирует: Интерстеллар (8.7)
```

---

#### Подсказки

- Чтобы считывать строку с клавиатуры, используйте `input()`
- Чтобы разбить строку на слова, примените `.split()`
- Чтобы перевести рейтинг из строки в число, используйте `float()`
